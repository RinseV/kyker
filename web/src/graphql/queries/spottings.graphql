query Spottings($animals: [Int!], $excludedAnimals: [Int!], $date: QueryDate) {
    spottings(
        animals: $animals
        excludedAnimals: $excludedAnimals
        date: $date
    ) {
        ...Spotting
    }
}
